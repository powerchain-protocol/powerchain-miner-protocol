# minerctl

`minerctl` is the Linux operator command for one renewable miner node.

Examples:

```bash
minerctl status
minerctl health
minerctl logs
minerctl queue
minerctl state
```

Commands intentionally avoid direct token minting or treasury operations.
