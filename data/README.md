# Data Contracts and Fixtures

This directory contains non-secret schemas, deterministic fixtures and deployment metadata.

Never store:
- device private keys
- API keys
- JWT secrets
- Solana authority keypairs
- customer meter credentials

`schema/proof-of-energy-v1.schema.json` is the canonical transport-level PoE shape.
