# @powerchain/api-client

Runtime-neutral TypeScript client for PowerChain `/api/v1`.

The client package contains HTTP request/response contracts only. Authentication tokens remain
owned by the calling application/runtime.
