# Debian

Use `linux/install.sh`. Debian 13+ is the reference generic Linux target.

For packaged deployments, install the systemd, sysusers and tmpfiles definitions from
the adjacent directories and package the Python wheel into `/opt/powerchain-miner/venv`.
