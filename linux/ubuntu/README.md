# Ubuntu Server

Use `linux/install.sh` on systemd-based Ubuntu Server. Ethernet is preferred for fixed
renewable-energy installations. Keep unattended security updates enabled, but stage
application/agent upgrades with signed artifacts and rollback.
